import { IconDefinition } from '@fortawesome/fontawesome-common-types';

export class AdministrationOptionModel {
	matIcon!: string;
	faIcon!: IconDefinition;
	isMatIcon!: boolean;
	title!: string;
	subtitle!: string;
	route!: string;
}
